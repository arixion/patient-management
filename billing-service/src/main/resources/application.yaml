spring:
  application:
    name: billing-service
  profiles:
    active: prod
  grpc:
    server:
      enabled: true
      port: 9001
server:
  port: 4002
  error:
    include-message: always
    whitelabel:
      enabled: true

